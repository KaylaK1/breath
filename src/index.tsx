// Main entry point of application - referenced in webpack.config.js
import React from 'react'
import ReactDOM from 'react-dom'
import './index.css'

ReactDOM.render(
  <h1>Webpage loaded</h1>,
  document.getElementById('app-root'),
)
